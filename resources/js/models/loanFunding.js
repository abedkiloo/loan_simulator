export default class LoanFunding {
    constructor(loan_id, lender_id, created_at, id) {
        this.loan_id = loan_id;
        this.lender_id = lender_id;
        this.created_at = created_at;
        this.id = id;
    }
}
